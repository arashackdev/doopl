# Binaries
*.exe
*.exe~
*.dll
*.so
*.so.*
*.dylib
doopl
doopl-mcp

# Test binary
*.test

# Coverage output
*.out

# Go workspace
go.work

# IDE & editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Build artifacts
bin/
dist/

# Task outputs
.task/

# Vendor
vendor/

# Test cache
.testcache/

# Claude Code memory (local-only)
.claude/

# Local development overrides
.env
.env.local

# Development notes (not shipped)
MILESTONES.md

# Docs policy: all docs must go in docs/ directory
# Scattered markdown files in root (should be in docs/) — NEVER commit to root
/*.md
!README.md
!CLAUDE.md
!LICENSE.md

# Scattered shell scripts in root (should be in docs/) — NEVER commit to root
/*.sh

# Generated mocks
internal/mocks/

# Test reports (temp files, don't commit)
test-report.json